% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwutilityfunctions.R
\name{empirical_fdr}
\alias{empirical_fdr}
\title{Calculate Empirical False Discovery Rate (FDR)}
\usage{
empirical_fdr(val, true_null_indices, ...)
}
\arguments{
\item{val}{A numeric vector of p-values or test statistics.}

\item{true_null_indices}{A numeric vector of indices indicating the positions
in \code{val} that correspond to true null hypotheses.}

\item{...}{Further arguments to \code{order()}; typically use
\code{decreasing = TRUE} if supplying test statistics.}
}
\value{
A numeric vector of FDR values corresponding to each threshold in the
  sorted \code{val}.
}
\description{
This function computes the empirical false discovery rate (FDR) for a given
set of p-values or similar scores, assuming a subset of indices represent
true null hypotheses.
}
\details{
The FDR is computed as the cumulative count of true nulls up to a
  given rank, divided by the rank itself. The function returns values sorted
  in ascending order.
}
\examples{
set.seed(123)
val <- runif(10)  # Generate 10 random uniform values
true_null_indices <- c(3, 6, 8)  # Indices of true nulls
empirical_fdr(val, true_null_indices)

}
